name: flida_auth_sdk_platform_interface
description: A common platform interface for the flida_auth_sdk plugin.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0
